spring:
  cloud:
    skipper:
      server:
        skipperHome: target/skipperTest
        packageRepositories:
         -
           name: test
           url: "classpath:/repositories/binaries/test"
