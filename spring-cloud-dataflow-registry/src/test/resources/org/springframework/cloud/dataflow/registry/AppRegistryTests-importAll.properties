source.foo				=classpath:/foo-source
sink.foo				=classpath:/foo-sink
source.foo.metadata		=classpath:/foo-source-metadata
source.bar				=classpath:/bar-source
source.bar.metadata		=classpath:/bar-source-metadata
