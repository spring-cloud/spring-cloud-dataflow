info:
  app:
    name: "@project.artifactId@"
    description: "@project.name@"
    version: "@project.version@"
spring:
  jpa:
    hibernate:
      ddl-auto: none
debug: true
logging:
  level:
    org:
      springframework:
        cloud:
          dataflow: debug
